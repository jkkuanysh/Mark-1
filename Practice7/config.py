"""Database configuration for the PhoneBook project.

Update these values so they match your local PostgreSQL setup.
"""

DB_CONFIG = {
    "host": "localhost",
    "port": 5432,
    "dbname": "phonebook_db",
    "user": "postgres",
    "password": "your_password_here",
}
